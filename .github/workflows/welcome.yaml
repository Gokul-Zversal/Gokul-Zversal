name: Post welcome comment
on:
  pull_request:
    types: [opened]
jobs:
  build:
    name: Post welcome comment
    runs-on: ubuntu-latest
    steps:
      - run: gh pr comment $PR_URL --body "Welcome to the repository!"
        env:
          GITHUB_TOKEN: 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
          PR_URL: ${{ github.event.pull_request.html_url }}
